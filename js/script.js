const {createApp} = Vue;

const app = createApp({
    name: 'Boolzapp',
    data: () => ({
        user: data.user,
        contacts: data.contacts
    }),

    computed: {

    },

    methods: {

    }
});

app.mount('#root');

console.log(data)